name: data
description: Data 로직 모음 - Core 를 실제 구현한 로직 모음입니다. API call, Persistent storage 등의 로직을 포함합니다.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core/
  util:
    path: ../util/

  dio: ^4.0.6
  equatable: ^2.0.3
  shared_preferences: ^2.0.15
  sprintf: ^6.0.0
  synchronized: ^3.0.0+2
  tuple: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.7
  faker: ^2.0.0
  flutter_lints: ^2.0.0
  mockito: ^5.1.0
  uuid: ^3.0.6
